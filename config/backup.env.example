# Copy to /etc/vaultwarden-backup/backup.env on the Pi (owner root:vwbackup, mode 0640).
# The real file is not in git: it holds the VPS address.

# VPS address in the tailnet (tailscale ip -4 on the VPS)
VPS_HOST=100.x.y.z
VPS_USER=deploy

# How many days of snapshots to keep
KEEP_DAYS=30
